# Dev/testing helper: put the Field Guide straight into the main hand so the
# e2e harness can open it with one right-click. Not referenced by tick.
item replace entity @s weapon.mainhand with modonomicon:modonomicon[modonomicon:book_id="cometcroft:field_guide"] 1
